name: Pass 45 strict runtime acceptance harness

on:
  pull_request:
  push:
    branches:
      - main
      - 'fix/runtime-acceptance-pass45-strict-harness-*'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pass45-strict-runtime-acceptance-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Verify strict Pass45 runtime acceptance harness
        run: python VERIFY_PASS45_STRICT_RUNTIME_ACCEPTANCE_HARNESS.py
      - name: Verify item16 exact production-path runtime binding
        run: |
          python -m py_compile VERIFY_PASS45_MANUAL_ACTION_RUNTIME.py VERIFY_PASS45_ITEM16_MANUAL_ACTION_RUNTIME_CONTRACT.py
          python VERIFY_PASS45_ITEM16_MANUAL_ACTION_RUNTIME_CONTRACT.py
      - name: Verify grenade type runtime evidence wiring
        run: python VERIFY_PASS45_GRENADE_TYPE_RUNTIME_EVIDENCE.py
