name: Runtime recovery Pass 15

on:
  pull_request:
  push:
    branches:
      - main
      - 'fix/runtime-acceptance-*'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: runtime-recovery-pass-15-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  verify-pass-15:
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - name: Checkout source without LFS payloads
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: false

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Verify runtime recovery Pass 15 source contracts
        shell: pwsh
        run: |
          $env:PYTHONUTF8='1'
          $env:PYTHONIOENCODING='utf-8'
          python .\VERIFY_RUNTIME_RECOVERY_PASS_15.py
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
