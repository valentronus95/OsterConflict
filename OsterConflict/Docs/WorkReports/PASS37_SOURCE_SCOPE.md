# Pass 37 source scope

This pass is intentionally limited to the repeated runtime regression shown after Pass 36:

1. prove that Museum architecture is physically visible near `MuseumAnchor`, not merely represented by owner tags;
2. move the preferred BASE into a closer, still-exterior front approach and face the player toward Museum;
3. recover visibly blank restored Stein weapon presentation without overwriting the already-correct AK;
4. preserve the existing >=30 FPS acceptance floor.

It does not claim exact authored texture restoration for payloads that do not contain those source materials, and it does not add unrelated decorative location layers.
