# S18B Release output

This directory is source-controlled documentation only. Actual packaged output is written to `Build/S18B/` by `Scripts/S18B/BuildS18B.ps1` and must not be committed into the source milestone archive.
